{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hub.accentcms.dev/schema/hub.schema.json",
  "title": "Registry descriptor (hub.json)",
  "description": "The machine-checkable form of m053 section 6.1. A client that does not implement contract_version refuses the registry outright rather than parsing best-effort, so the version is a const and not a minimum.",
  "type": "object",
  "additionalProperties": false,
  "required": ["contract_version", "name", "kinds", "generated_at"],
  "properties": {
    "contract_version": {
      "description": "2 for this contract.",
      "const": 2
    },
    "name": {
      "description": "Human-readable registry name.",
      "type": "string",
      "minLength": 1
    },
    "kinds": {
      "description": "Kinds served. A self-hosted registry may serve a subset.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "enum": ["plugin", "theme", "template"] }
    },
    "generated_at": {
      "description": "When the generator wrote this descriptor.",
      "$ref": "#/$defs/timestamp"
    }
  },
  "$defs": {
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}([.][0-9]+)?(Z|[+-][0-9]{2}:[0-9]{2})$"
    }
  }
}
